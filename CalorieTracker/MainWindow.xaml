<Window x:Class="CalorieTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalorieTracker"
        mc:Ignorable="d"
        Title="MainWindow" Height="750" Width="1350">
   <Grid>
    <Grid.RowDefinitions>
        <RowDefinition Height="50"/> <RowDefinition Height="*"/>  </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="#3F51B5" Padding="10,0">
        <Grid>
            <TextBlock Text="Calorie Tracker 3000" Foreground="White" VerticalAlignment="Center" FontWeight="Bold" FontSize="16"/>
            <Button x:Name="LogoutButton" Content="LOGOUT" HorizontalAlignment="Right" VerticalAlignment="Center" 
                    Padding="10,5" Background="#F44336" Foreground="White" BorderThickness="0" Click="LogoutButton_Click"/>
        </Grid>
    </Border>

    <TabControl Grid.Row="1">
        <TabItem Header="Daily Tracking">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Today's Meals" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <DataGrid x:Name="MealsDataGrid" AutoGenerateColumns="False" Height="400" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Meal Type" Binding="{Binding Type}" Width="*"/>
                            <DataGridTextColumn Header="Product" Binding="{Binding Product}" Width="*"/>
                            <DataGridTextColumn Header="Weight" Binding="{Binding Weight}"/>
                            <DataGridTextColumn Header="Calories" Binding="{Binding CaloriesCalculated}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button x:Name="DeleteMealButton" Content="Delete Selected" Background="#E91E63" Foreground="White" Margin="0,10,0,0" Click="DeleteMealButton_Click"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Add Meal" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock Text="Type:"/>
                    <TextBox x:Name="MealTypeTextBox" Margin="0,0,0,10"/>
                    <TextBlock Text="Select Product:"/>
                    <ComboBox x:Name="ProductsComboBox" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,0,10"/>
                    <TextBlock Text="Weight (g):"/>
                    <TextBox x:Name="WeightTextBox" Margin="0,0,0,20"/>
                    <Button x:Name="AddMealButton" Content="Add to Journal" Height="40" Background="#4CAF50" Foreground="White" Click="AddMealButton_Click"/>
                </StackPanel>
            </Grid>
        </TabItem>

        <TabItem Header="Product Manager">
            <Grid Margin="10">
                <TextBlock Text="Why do Java developers wear glasses? Because they can't C#" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Foreground="Gray"/>
            </Grid>
        </TabItem>
    </TabControl>
</Grid>
</Window>
